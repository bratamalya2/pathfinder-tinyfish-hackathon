{
  "name": "pathfinder-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel -r dev",
    "build": "pnpm -r build",
    "start": "pnpm --filter pathfinder-api start",
    "test": "pnpm -r test"
  }
}
